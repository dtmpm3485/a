[package]
name = "privacypic_core"
version = "1.0.0"
edition = "2021"

[lib]
name = "privacypic_core"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
